/** @format */

import { RenderViewName } from '../model'

export const IconForRendersView: Record<RenderViewName, string> = {
    'flow-builder': 'fa-project-diagram',
    'grapejs-editor': 'fa-palette',
    'raw-editor': 'fa-code',
    runner: 'fa-eye',
}
